#include <stdio.h>

int main() {
    int i = 1;

    // Loop para imprimir a tabuada do 6
    while (i <= 10) {
        printf("6 x %d = %d\n", i, 6 * i);
        i++; // Incrementa o contador
    }

    return 0;
}